[project]
name = "save-and-resurface"
version = "0.1.0"
description = "Sauvegarde et resurface de contenu web avec synthèses Claude"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "streamlit>=1.40.0",
    "json-repair>=0.30.0",
]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
]
